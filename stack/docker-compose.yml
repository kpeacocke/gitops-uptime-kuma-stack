---
x-logging: &default-logging
  driver: json-file
  options:
    max-size: "10m"
    max-file: "5"
    tag: "{{.Name}}"

services:
  uptime-kuma:
    image: 'louislam/uptime-kuma:${UPTIME_KUMA_VERSION:-2.5.3}'
    container_name: uptime-kuma
    labels:
      com.ambitiouscake.managed-by: "portainer-gitops"
      com.ambitiouscake.environment: "production"
      com.ambitiouscake.stack: "uptime-kuma"
      com.centurylinklabs.watchtower.enable: "true"
    environment:
      - TZ=${TZ:-Australia/Sydney}
    # Host networking lets Kuma monitor the NAS loopback-only service ports
    # without hairpinning through Cloudflare and the Synology reverse proxy.
    network_mode: host
    volumes:
      - '${UPTIME_KUMA_DATA_PATH:?Set UPTIME_KUMA_DATA_PATH in .env}:/app/data'
    restart: unless-stopped
    logging: *default-logging
    security_opt:
      - no-new-privileges:true

  uptime-kuma-backup:
    image: 'alpine:3.22'
    container_name: uptime-kuma-backup
    labels:
      com.ambitiouscake.managed-by: "portainer-gitops"
      com.ambitiouscake.environment: "production"
      com.ambitiouscake.stack: "uptime-kuma"
      com.centurylinklabs.watchtower.enable: "false"
    environment:
      - TZ=${TZ:-Australia/Sydney}
      - BACKUP_RETENTION_DAYS=${BACKUP_RETENTION_DAYS:-30}
    command:
      - /bin/sh
      - -ec
      - |
        apk add --no-cache sqlite
        backup_dir=/backups/uptime-kuma-backups
        mkdir -p "$${backup_dir}"
        while true; do
          stamp=$$(date -u +%Y-%m-%dT%H-%M-%SZ)
          target="$${backup_dir}/kuma-$${stamp}.db"
          sqlite3 /source/kuma.db ".backup '$${target}'"
          test "$$(sqlite3 "$${target}" 'PRAGMA integrity_check;')" = ok
          (cd "$${backup_dir}" && sha256sum "$$(basename "$${target}")" > "$$(basename "$${target}").sha256")
          find "$${backup_dir}" -type f \( -name 'kuma-*.db' -o -name 'kuma-*.db.sha256' \) -mtime "+$${BACKUP_RETENTION_DAYS}" -delete
          date -u +%Y-%m-%dT%H:%M:%SZ > "$${backup_dir}/.last-success"
          echo "Verified backup created: $${target}"
          sleep 86400
        done
    volumes:
      - '${UPTIME_KUMA_DATA_PATH:?Set UPTIME_KUMA_DATA_PATH in .env}:/source:ro'
      - '${UPTIME_KUMA_BACKUP_PATH:?Set UPTIME_KUMA_BACKUP_PATH in .env}:/backups'
    restart: unless-stopped
    logging: *default-logging
    security_opt:
      - no-new-privileges:true
    healthcheck:
      test:
        - CMD-SHELL
        - 'test -f /backups/uptime-kuma-backups/.last-success && test $$(find /backups/uptime-kuma-backups/.last-success -mmin -1800 | wc -l) -eq 1'
      interval: 1m
      timeout: 10s
      retries: 2
      start_period: 5m
